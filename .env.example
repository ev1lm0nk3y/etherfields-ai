# Etherfields AI Rule Master Configuration Template
# Copy this to '.env' and modify as needed.

# Absolute path to the custom directory where logs, caches, rulebook pages, and models are stored.
# Tilde (~) or environment variables like $HOME can be used; they will be expanded.
ETHERFIELDS_CUSTOM_DIR=~/.local/etherfields-ai

# Enable voice assistant and wake-word listener (True/False)
ENABLE_VOICE=False

# The active wake-word model name (stored inside ETHERFIELDS_CUSTOM_DIR/models/)
WAKE_WORD_MODEL=jarvis.onnx
